<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="windowActionBar">false</item>
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:colorBackground">@android:color/background_light</item>
        <item name="android:actionMenuTextColor">@android:color/darker_gray</item>
        <item name="android:textColorPrimary">@android:color/tertiary_text_light</item>
        <item name="android:textColorSecondary">@android:color/background_light</item>
        <item name="android:textColorPrimaryInverse">@android:color/background_light</item>
        <!--<item name="android:textColorSecondaryInverse">@color/cardview_light_background</item>-->
    </style>

</resources>
